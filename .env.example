# Microsoft Clarity project id for ashmitbohora.com (create at clarity.microsoft.com, one project per property).
# Leave unset and no Clarity script is emitted. Build reads this at `astro build` time, so it must be in .env locally before deploying.
PUBLIC_CLARITY_PROJECT_ID=
